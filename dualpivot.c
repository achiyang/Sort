#include "sortutils.h"
#include "dualpivot.h"

#define THRESHOLD 64

static void sortCore(char *arr, char *temp, size_t left, size_t right, size_t size_element, compareFunc compare) {

}

void dualPivotQuickSort(void *arr, size_t num_elements, size_t size_element, compareFunc compare) {

}
